spec:
  name: palmsure
  static_sites:
    - name: palmsure-web
      github:
        repo: HunchoKing/Palmsure
        branch: main
        deploy_on_push: true
      source_dir: /
      build_command: corepack enable pnpm && pnpm install && ng build --configuration production --output-mode static
      output_dir: dist/palmsure-corp-v2/browser
      routes:
        - path: /
